{
  "name": "hoja-vida-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "app.js", // ✅ Cambiar a app.js
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "build": "cd frontend && npm install && npm run build", // ✅ Añadir build
    "heroku-postbuild": "npm run build", // ✅ Script para Heroku
    "init-codigos": "node scripts/inicializarCodigos.js",
    "crear-admin": "node scripts/crearAdmin.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5", // ✅ Asegurar que cors está incluido
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.1",
    "pdfkit": "^0.15.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
